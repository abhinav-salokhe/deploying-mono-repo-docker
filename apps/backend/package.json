{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "db": "*",
    "@types/express": "^5.0.6",
    "express": "^5.2.1"
  },
  "scripts": {
    "generate:db": "cd packages/db && bunx prisma generate",
    "start:backend": "cd apps/backend && bun run index.ts"
  }
}
